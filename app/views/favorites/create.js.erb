$(".favorite-btn").html("<%= j(render 'favorites/favorite-btn', book: @book) %>");
<!--↓@book.idを入れることで一つのbookを指定している-->
$("#book_<%= @book.id %>").html("<%= j(render 'favorites/favorite-btn', book: @book) %>");
